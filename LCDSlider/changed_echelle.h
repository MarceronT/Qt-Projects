#ifndef CHANGED_ECHELLE_H
#define CHANGED_ECHELLE_H

#include <QWidget>

class Changed_Echelle : public QWidget
{
    Q_OBJECT
public:
    explicit Changed_Echelle(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // CHANGED_ECHELLE_H
